<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:id="@+id/container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:layout_margin="24dp"
            android:layout_marginTop="32dp"
            android:layout_marginBottom="32dp"
            android:padding="8dp"
            android:background="@android:color/transparent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Gambar kota -->
            <ImageView
                android:id="@+id/ivGambarKota"
                android:layout_width="300dp"
                android:layout_height="200dp"
                android:scaleType="centerCrop"
                android:layout_marginBottom="16dp"
                android:contentDescription="Gambar Kota" />

            <!-- Nama Kota -->
            <TextView
                android:id="@+id/tvNamaKota"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Nama Kota"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="#0d0d0c"
                android:layout_gravity="center_horizontal"
                android:paddingBottom="16dp" />

            <!-- Ciri khas lainnya -->
            <TextView
                android:id="@+id/tvMakanan"
                style="@style/CiriKhasStyle" />

            <TextView
                android:id="@+id/tvPakaian"
                style="@style/CiriKhasStyle" />

            <TextView
                android:id="@+id/tvSenjata"
                style="@style/CiriKhasStyle" />

            <TextView
                android:id="@+id/tvRumah"
                style="@style/CiriKhasStyle" />

            <TextView
                android:id="@+id/tvMusik"
                style="@style/CiriKhasStyle" />

            <TextView
                android:id="@+id/tvKesenian"
                style="@style/CiriKhasStyle" />

            <TextView
                android:id="@+id/tvBahasa"
                style="@style/CiriKhasStyle" />

            <TextView
                android:id="@+id/tvIkon"
                style="@style/CiriKhasStyle" />

            <Button
                android:id="@+id/btnBackKota"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Kembali ke menu utama"
                android:textAllCaps="false"
                android:layout_marginTop="16dp"
                android:backgroundTint="#757575"
                android:textColor="#FFFFFF" />

            <Button
                android:id="@+id/btnPrevCiriKhas"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="Kembali ke halaman sebelum nya"
                android:backgroundTint="#9E9E9E"
                android:textColor="#FFFFFF" />


            <!-- Tombol Next -->
            <Button
                android:id="@+id/btnNextKota"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Lanjut ke Kota Berikutnya"
                android:textAllCaps="false"
                android:layout_marginTop="24dp"
                android:backgroundTint="#FF9800"
                android:textColor="#FFFFFF" />
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
